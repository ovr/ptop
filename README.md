Perfect top aka ptop
====================

## How to develop

First, clone the project `git clone git@github.com:ovr/ptop.git`

Install deps `npm install`

Run electron `./node_modules/.bin/electron .`
